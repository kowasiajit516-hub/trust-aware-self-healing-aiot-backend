[pytest]
# Explicitly set the default asyncio fixture loop scope to eliminate warnings
asyncio_default_fixture_loop_scope = function

# Formalize test discovery options
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Add backend/ to sys.path so `from config import settings` etc. resolve
pythonpath = .